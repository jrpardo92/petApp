import 'package:flutter/material.dart';

class Pet3Screen extends StatelessWidget {
  const Pet3Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pet3Screen'),
      ),
    );
  }
}
